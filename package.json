{
  "name": "spoteasy-tour-automation",
  "version": "2.0.0",
  "description": "SpotEasy tour request automation — CSV-driven, human-like timing",
  "main": "tour-server.js",
  "scripts": {
    "start": "node tour-server.js",
    "dev": "node tour-server.js",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "csv-parser": "^3.0.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
